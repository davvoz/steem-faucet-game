# DISATTIVATO: Questo workflow è stato disattivato per utilizzare solo GitHub Pages
# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: DISABLED - Firebase Hosting on PR
on:
  # Nessun trigger automatico - workflow disabilitato
  workflow_dispatch:
    # Solo esecuzione manuale se necessario

permissions:
  checks: write
  contents: read
  pull-requests: write
  
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Questo workflow è stato disabilitato. Utilizziamo solo GitHub Pages per il deployment."
